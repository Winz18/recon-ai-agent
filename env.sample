# Google Cloud Configuration
# Required for AG2 Vertex AI integration
GOOGLE_PROJECT_ID=your-google-project-id
GOOGLE_REGION=us-central1

# Target Configuration
TARGET_DOMAIN=google.com

# Optional: Google Cloud Authentication
# If you're using service account key file, place it in credentials/application_default_credentials.json
# Otherwise use: gcloud auth application-default login
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/application_default_credentials.json

# Docker User Mapping (automatically set by fix-permissions.sh)
# Prevents permission issues with mounted volumes
UID=1000
GID=1000

# Optional: Redis Configuration (if you want to customize Redis behavior)
# REDIS_URL=redis://redis:6379/0

# Optional: AI Model Configuration
# MODEL_ID=gemini-2.5-pro-preview-05-06

# Optional: Logging Configuration
# LOG_LEVEL=INFO
# LOG_FORMAT=json 